export interface SphericalCoordinate {
  radius: number;
  theta: number; // Angle between radial line and polar axis
  phi: number; // Angle of radial line around polar axis. Where 0 is the x axis
}
